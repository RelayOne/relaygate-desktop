I found unexpected working-tree changes that are not in the two commits you asked me to review: `electron-builder.yml` is modified and there are untracked `.work/reviews/` and `assets/icon.ico`. I paused per safety rules.

Reply with one option so I can continue:
1. Review `HEAD` only (`6b37ff3`) and ignore uncommitted changes.
2. Include uncommitted changes in the review.